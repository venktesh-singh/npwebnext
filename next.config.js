const nextConfig = {
    distDir: 'build',
    images: {
      domains: ['npkohlercompaignapi.onrender.com', 'http://localhost:4001'], // Add the external domain here
    },
  }
  
  module.exports = nextConfig

